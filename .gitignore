# Android / Gradle (scope to Gradle output dirs only — do NOT swallow .eas/build)
.gradle/
/build/
app/build/
local.properties
*.apk
*.aab
*.keystore
!debug.keystore

# Prebuilt proot binaries are fetched by tools/fetch-proot.sh, not committed.
app/src/main/jniLibs/*/libproot.so

# Node / EAS
node_modules/

# IDE
.idea/
*.iml
.DS_Store
